package org.example.repository.simulacao;

import org.example.model.Auditoria;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SimulacaoAuditoriaRepository extends JpaRepository<Auditoria, Long> {
}

